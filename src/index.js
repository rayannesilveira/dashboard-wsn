import React from 'react';
import ReactDOM from 'react-dom/client';
import { RoutesCompontent } from './routes';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RoutesCompontent/>
  </React.StrictMode>
);

